# Used by docker-compose. Copy to .env and fill in.
# (For non-Docker local dev, configure flask-server/.env instead.)

GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash-lite

# Optional: only needed for the Notion source.
NOTION_TOKEN=
